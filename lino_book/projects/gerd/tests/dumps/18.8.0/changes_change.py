# -*- coding: UTF-8 -*-
logger.info("Loading 0 objects to table changes_change...")
# fields: id, time, type, user, object_type, object_id, master_type, master_id, diff, changed_fields

loader.flush_deferred_objects()
